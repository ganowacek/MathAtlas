import { createRoot } from 'react-dom/client';
import MathAtlasApp from '@/app/components/MathAtlasApp';
import '@/app/globals.css';

createRoot(document.getElementById('root')!).render(<MathAtlasApp />);
